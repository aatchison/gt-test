apiVersion: v1
kind: Service
metadata:
  name: gttest-app
  namespace: gttest
spec:
  type: NodePort
  selector:
    app: gttest-app
  ports:
    - port: 80
      targetPort: 3000
      nodePort: 30000
